// import { Training } from "@/components/workout/training";
// import { getExercisesByMatchId } from "@/lib/services/trainingPlans";

// export async function TrainingListColumn() {
//   // const { data, success } = await getExercisesByMatchId(matchId);

//   // if (!success || !data) return null;

//   return <Training exercises={[]} />;
// }
